server:
  port: 10015

sw:
  nacos:
    server-addr: ${sw.nacos.ip}:8848

spring:
  application:
    name: market-center
  cloud:
    nacos:
      discovery:
        server-addr: ${sw.nacos.server-addr}
      config:
        server-addr: ${sw.nacos.server-addr}

