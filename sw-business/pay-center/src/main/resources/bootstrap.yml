server:
  port: 10018

sw:
  nacos:
    server-addr: ${sw.nacos.ip}:8848

spring:
  application:
    name: pay-center
  cloud:
    nacos:
      discovery:
        server-addr: ${sw.nacos.server-addr}
      config:
        server-addr: ${sw.nacos.server-addr}


