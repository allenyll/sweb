server:
  port: 10011

sw:
  nacos:
    server-addr: localhost:8848

spring:
  application:
    name: order-center
  cloud:
    nacos:
      discovery:
        server-addr: ${sw.nacos.server-addr}
      config:
        server-addr: ${sw.nacos.server-addr}


