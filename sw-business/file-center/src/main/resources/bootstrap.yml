server:
  port: 10014

sw:
  nacos:
    server-addr: ${sw.nacos.ip}:8848

spring:
  application:
    name: file-center
  cloud:
    nacos:
      discovery:
        server-addr: ${sw.nacos.server-addr}
      config:
        server-addr: ${sw.nacos.server-addr}


