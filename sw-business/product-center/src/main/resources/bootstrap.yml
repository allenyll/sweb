server:
  port: 10013

sw:
  nacos:
    server-addr: localhost:8848

spring:
  application:
    name: product-center
  cloud:
    nacos:
      discovery:
        server-addr: ${sw.nacos.server-addr}
      config:
        server-addr: ${sw.nacos.server-addr}


