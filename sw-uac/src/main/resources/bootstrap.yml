server:
  port: 10002

sw:
  nacos:
    server-addr: ${sw.nacos.ip}:8848

spring:
  application:
    name: sw-uac
  cloud:
    nacos:
      discovery:
        server-addr: ${sw.nacos.server-addr}
      config:
        server-addr: ${sw.nacos.server-addr}


