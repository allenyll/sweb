server:
  port: 10020

sw:
  nacos:
    server-addr: localhost:8848

spring:
  application:
    name: sw-job
  cloud:
    nacos:
      discovery:
        server-addr: ${sw.nacos.server-addr}
      config:
        server-addr: ${sw.nacos.server-addr}


