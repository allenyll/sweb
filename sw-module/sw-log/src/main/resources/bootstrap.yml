server:
  port: 10003

sw:
  nacos:
    server-addr: ${sw.nacos.ip}:8848

spring:
  application:
    name: sw-log
  cloud:
    nacos:
      discovery:
        server-addr: ${sw.nacos.server-addr}
      config:
        server-addr: ${sw.nacos.server-addr}


